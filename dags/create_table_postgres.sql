CREATE TABLE IF NOT EXISTS test (
    uuid VARCHAR PRIMARY KEY,
    title VARCHAR,
    full_name VARCHAR,
    email VARCHAR,
    phone VARCHAR,
    address VARCHAR,
    date_of_birth DATE,
    age INTEGER
);
